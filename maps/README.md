# maps

Empty on purpose. Populate it with the bundled maps:

```bash
unswbc maps maps
```

Custom maps (from https://game.battlecode.au/map-editor or written by
hand as `.map` files) also go here, alongside the bundled ones.
